interface:
  display_name: "Merge Reviewer"
  short_description: "Review Git merges and branch differences"
  default_prompt: "Use $merge-reviewer to review the current local branch against a remote default branch or two explicit Git refs."

policy:
  allow_implicit_invocation: true
